notas = []

while True:
    x = float(input())
    if 0 <= x <= 10:
        notas.append(x)
        if len(notas) == 2:
            print('media = {:.2f}'.format(sum(notas) / 2))
            break
    else:
        print('nota invalida')
