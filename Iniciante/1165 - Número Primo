casos = int(input())
numeros = []

for i in range(casos):
    x = int(input())
    for k in range(1, x+1):
        if x % k == 0:
            numeros.append(k)
    if len(numeros) == 2:
        print(x, 'eh primo')
    else:
        print(x, 'nao eh primo')
    numeros.clear()
